export interface AppState {
  gameSavePath: string;
}

export interface Person {
  name: string;
  age: number;
}

export interface Card {
  id: number; 
  title: string;
  identifier: string;
}

export interface Relic {
  id: number;
  title: string;
  identifier: string;
}